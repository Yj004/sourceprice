# Google Sheets (required for API server)
GOOGLE_SHEET_ID=your-google-sheet-id

# Local dev: path to service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./credentials/google-service-account.json

# Vercel / serverless: paste full minified JSON (use instead of file path)
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

PORT=3002
# Comma-separated. On Vercel, preview/production URLs are added automatically.
BACKEND_CORS_ORIGINS=http://localhost:5173,https://your-app.vercel.app

# Frontend (optional — defaults to /api via Vite proxy)
VITE_API_BASE=/api

# Auth — first super admin seeded when user-manage tab is empty
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_BOOTSTRAP_PASSWORD=change-me
JWT_SECRET_KEY=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Email — product update alerts (all changed fields; brand routing in server/emailService.js)
# EMAIL_CC_UPDATER=true  → also sends a copy to the logged-in user who saved
# Always: akshit.mittal@avaipl.com, anirudh.bansal@avaipl.com, mukul.bansal@avaipl.com
EMAIL_FROM=sourceprice@avaipl.com
EMAIL_ENABLED=true

# SMTP (required to actually send emails)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username@avaipl.com
SMTP_PASS=your-smtp-app-password
